import 'package:flutter/material.dart';

class AddAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add Admin')),
      body: Center(child: Text('Add admin form goes here')),
    );
  }
}
